This role generates a bigass hugo static web page and uploads it to a web server. This was originally grabbed from bonovoxly who is using a BSD license on his work.
Here's hoping the changes made are going to get this to work on a local development environment instead of using AWS.

Requires lunr-hugo for search, but will likely fail gracefully.
